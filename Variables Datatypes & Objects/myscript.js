console.log("hello saxy boii");

let j = 13;
console.log("The value of j is :" + j);

{
    // console.log(z);
    let z = 20;
    console.log(j);
}
console.log(j);

const f = 90;
// Value in let can be changed but not in const i.e value in const cannot be changed it will be fixed and it will be an error if we try to change the value of const
